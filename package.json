{
  "dependencies": {
    "path-to-regexp": "^6.2.1"
  },
  "name": "named-routes",
  "description": "Example: ```ts const ROUTES = {    foo: include('/foo/', {      bar: 'bar/',      scope: include('scope/:param', { // <- Note the missing trailing slash.        baz: 'baz', // <- Note the missing trailing slash.        qux: 'qux/'      })    }) } ```",
  "version": "1.0.0",
  "main": "index.js",
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/emilte/named-routes.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/emilte/named-routes/issues"
  },
  "homepage": "https://github.com/emilte/named-routes#readme"
}
